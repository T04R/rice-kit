pacman -S xclip wl-clipboard

mkdir -p ~/.config/helix/runtime/themes
cp hesi.toml ~/.config/helix/runtime/themes/hesi.toml
cp config.toml ~/.config/helix/config.toml

#if want use hx instead of helix in arch
mv /usr/bin/helix /usr/bin/hx
